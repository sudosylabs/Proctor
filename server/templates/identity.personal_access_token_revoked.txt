{{/*
Template properties (server/app/mail renderer):
  .Copy.Subject — localized message subject
  .Copy.Preheader — localized inbox preview
  .Copy.Heading — localized visible heading
  .Copy.Body — localized primary message
  .Copy.ActionLabel — localized action label; empty when no action is offered
  .Copy.Footer — localized closing and safety copy
  .Copy.PersonalAccessToken — localized labels and closed scope names
  .ActionURL — validated HTTPS action URL; empty when no action is offered
  .PersonalAccessToken.Description — bounded PAT description; never a credential
  .PersonalAccessToken.ExpiresAt — UTC RFC 3339 expiry
  .PersonalAccessToken.ActionAt — UTC RFC 3339 transition time
  .PersonalAccessToken.Scope — localized Institution or Academic Unit context
  .PersonalAccessToken.ActionCount — bounded count; never the complete scopes
*/}}
{{.Copy.Heading}}

{{.Copy.Body}}

{{.Copy.PersonalAccessToken.DescriptionLabel}}: {{.PersonalAccessToken.Description}}
{{.Copy.PersonalAccessToken.ExpiresAtLabel}}: {{.PersonalAccessToken.ExpiresAt}}
{{.Copy.PersonalAccessToken.ActionAtLabel}}: {{.PersonalAccessToken.ActionAt}}
{{.Copy.PersonalAccessToken.ScopeLabel}}: {{.PersonalAccessToken.Scope}}
{{.Copy.PersonalAccessToken.ActionCountLabel}}: {{.PersonalAccessToken.ActionCount}}

{{.Copy.Footer}}
