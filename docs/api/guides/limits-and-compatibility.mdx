---
title: Limits and Compatibility
description: Design Proctor clients for bounded inputs, rate controls, the pre-release compatibility boundary, and safe observability.
audience: api-consumer
maturity: preview
sidebar_position: 8
---

# Limits and Compatibility

Proctor rejects input before unbounded parsing. Treat every request body,
collection page, upload, log entry, and retry loop as bounded even when an
installation has not published a narrower local policy.

## Client Limits

- Use the endpoint's documented `limit` range; never request or assume an
  unbounded collection.
- Stream file content where the client library permits it and enforce the
  endpoint's maximum before upload.
- Put only documented fields in request objects. Unknown JSON fields are
  rejected.
- Send exactly one JSON value and the declared media type.
- Apply exponential backoff with jitter to documented transient failures and
  realtime reconnects.
- Respect an installation's response headers and stable error codes when it
  applies rate or concurrency controls. Do not evade them with parallel
  credentials.

The public multipart hard boundary is 100 MiB plus 64 KiB of overhead, but a
specific route or installation policy can be lower. No generic maximum should
override the endpoint schema.

## Compatibility Within `/api/v1`

Proctor has not yet made its first supported-release API commitment. During
this pre-release period, the `/api/v1` contract may change in place when the
server, generated contract, and compatible first-party clients are activated
together. There is no parallel version created solely to preserve an older
development contract. Pin the reviewed OpenAPI artifact and server build used
by every integration instead of treating the path suffix as a support promise.

After the first supported release, established meanings evolve additively
unless an explicit compatibility decision introduces another version and a
migration path. Additive evolution can include operations, optional request
fields, response fields, open enum values, and new stable error codes. A robust
client in either period:

- ignores unknown response object fields;
- handles unknown error and realtime event types safely;
- does not exhaustively switch over an open value without a fallback;
- sends only fields supported by the contract version it targets; and
- does not derive meaning from opaque identifiers, cursors, ETags, or tokens.

After that supported-release boundary, removing a field, making an input
required, or changing established behavior requires an explicit compatibility
decision. The checked-in OpenAPI contract is authoritative for a build; pin or
archive the contract used to generate a client so every update is deliberate.

## Observability Without Exposure

Safe client telemetry includes the HTTP method, route template, status,
duration, stable `code`, and `request_id`. Do not log bearer, DPoP, or refresh
credentials; DPoP proofs or nonces; cookies; CSRF values; invitation secrets;
Attempt credentials; student answers; raw Exam content; or unbounded
user-controlled fields.
