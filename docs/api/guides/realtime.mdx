---
title: Realtime and Replay
description: Connect to Proctor WebSocket events, authorize subscriptions, and recover from bounded replay gaps.
audience: api-consumer
maturity: preview
sidebar_position: 7
---

import Link from '@docusaurus/Link';

# Realtime and Replay

Proctor exposes one authenticated WebSocket endpoint at `/api/v1/websocket`.
Realtime delivery accelerates an interface; durable HTTP resources remain the
authority.

Browser connections use the Web Session cookie and must present the
installation's exact allowed Origin. A Web client using an allowed Web Session
access credential may use bearer transport. Proctor Desktop instead presents
its Desktop Session as `Authorization: DPoP …` with a fresh proof for the
WebSocket handshake and current server nonce; a Desktop credential is never a
bearer credential. Non-browser clients may omit Origin.

The connection handshake authenticates one Session but does not grant every
subscription. Each subscription action and exact resource is authorized
separately. Candidate admission additionally requires the registered-key
Desktop Session and the exact Desktop compatibility inputs enforced by the
Attempt protocol.

See <Link to="/api/reference/connect-web-socket">Connect WebSocket</Link> for the current
handshake parameters and protocol schemas.

## Resume After Disconnect

Replay is bounded and node-local. Retain the most recent sequence or replay
cursor exactly as the protocol specifies, then offer it when reconnecting to
the appropriate node. A successful replay fills only the available bounded
gap.

If the server reports that replay is unavailable or incomplete:

1. Stop applying incremental events for the affected scope.
2. Refetch the authoritative resource or collection over HTTP.
3. Replace local derived state with that snapshot.
4. Establish a fresh realtime position and resume incremental processing.

Never treat a missing event as evidence that durable state did not change.
Cluster fan-out and WebSocket publication occur after the durable commit and
are best-effort effects.

## Protocol Errors

After upgrade, protocol errors use stable typed WebSocket envelopes rather
than RFC 9457 Problem Details. Handle an unknown event or error type
defensively, preserve ordering within its documented scope, and reconnect with
bounded backoff. Avoid tight reconnect loops after authentication or
authorization rejection. A failed DPoP handshake can return a replacement
`DPoP-Nonce`; reconnect with a newly signed proof rather than replaying the
upgrade request byte-for-byte.
