# The OpenAPI theme's transitive postman-code-generators installer launches
# foreign package managers inside node_modules and is not deterministic.
# Its published runtime is complete without lifecycle scripts; the codegen
# smoke test and clean production build enforce that assumption.
ignore-scripts=true
