---
title: Change Persistence and Migrations
description: Evolve Proctor Store contracts, PostgreSQL operations, constraints, caches, and the current pre-release schema baseline safely.
audience: developer
maturity: preview
slug: /developers/persistence/
---

# Change Persistence and Migrations

PostgreSQL is authoritative for durable Proctor application state. Persistence
does more than execute queries: it owns locking, database-time decisions,
constraints, optimistic concurrency, and named atomic guarantees required by
application use cases.

Redis, in-process caches, cluster messages, and runtime projections remain
disposable and reconstructible. Never make them the sole copy of state that
must survive restart or failover.

## Start from the Application Guarantee

Before writing SQL, name the complete operation the application needs. A
cross-model mutation should become a typed, named Store operation such as a
bootstrap, transfer, command-outcome, or lifecycle transition—not a generic
transaction callback exposed to application code.

The application owns policy and transaction intent. The SQL adapter owns:

- row and advisory locking;
- PostgreSQL-time evaluation;
- uniqueness and referential constraints;
- revision and idempotency fences;
- statement ordering;
- commit or rollback; and
- translation into portable Store outcomes.

Raw SQL transactions, SQL handles, and `WithTransaction(func(Store))` do not
cross into `server/app`.

## Implement a Store Change

1. Add or refine the smallest cohesive Store contract under `server/store`.
2. Add conformance cases that express observable results and race guarantees.
3. Implement the contract in `server/store/sqlstore` with explicit bounded
   queries and constraints.
4. Update memory or test adapters used by consumers; do not weaken the contract
   to make a fake easier to write.
5. Add application integration coverage through the real composition graph
   when transaction behavior affects a use case.
6. Update metrics through the existing bounded recorder or wrapper rather than
   adding query text, resource IDs, or error text as labels.

Read paths must remain bounded and authorization-aware where visibility is
part of the query. Avoid loading an unrestricted collection and filtering it
in application memory.

## Use Precise Lookup Semantics

Store methods use three distinct absence contracts:

| Operation | Meaning when no record exists |
| --- | --- |
| `Get` | Return `store.ErrNotFound` |
| `Find` | Return the explicit `found` value as false |
| `List` | Return an empty collection |

`(nil, nil)` never means “not found.” A returned record is rehydrated into a
domain type and validated before it crosses the adapter boundary. An invalid
persisted record is an integrity failure, not an absent record.

Store contracts return domain values or explicit Store-owned projections and
aggregate results. Keep SQL rows, nullable driver types, query builders,
column names, and transaction handles private to `sqlstore`.

## Name the Atomic Guarantee

A named Store operation is valuable when its name and parameters expose the
complete race that must be resolved. For example, a conditional replacement
may need to:

1. lock the current aggregate;
2. resolve an existing idempotent outcome;
3. compare the expected revision;
4. write the new state;
5. write required audit;
6. retain a bounded replay result; and
7. commit all of those facts together.

The application prepares the policy-bearing inputs. The Store contract states
what wins under concurrency. The SQL implementation chooses row locks,
advisory locks, constraints, and statement order.

Do not split a guarantee into a sequence of public Store calls merely because
each call would be smaller. Another node can act between those calls.

## Use Constraints and Locks Together

Choose the narrowest PostgreSQL mechanism that protects the invariant:

- a unique, foreign-key, check, or exclusion constraint for facts that must be
  true regardless of caller;
- `SELECT … FOR UPDATE` when one current row serializes a transition;
- a transaction-scoped advisory lock for a bounded namespace that has no
  natural row yet; and
- `SKIP LOCKED` for work claiming or bounded maintenance where several nodes
  may safely divide eligible rows.

Application prechecks can improve error quality but never replace the
constraint or lock that closes the race. Translate expected constraint
outcomes into typed Store errors; preserve unexpected driver detail only in the
wrapped internal cause.

Keep a canonical lock order when a transaction touches several aggregates.
Review existing named operations that touch the same rows before adding
another order.

## Separate Revisions, Time, and Idempotency

These mechanisms answer different questions:

| Mechanism | Question |
| --- | --- |
| Explicit revision | Is this still the exact state the caller inspected? |
| PostgreSQL time | Is a deadline, lease, or effective period current at commit? |
| Idempotency key | Has this authenticated semantic command already completed? |
| Unique constraint | Can two durable records with this identity coexist? |

Do not use timestamps as optimistic revisions. Do not treat an idempotency key
as permission to skip current authorization. An exact replay returns its
retained result only after the current use case has repeated the checks required
by its contract.

Retry only operations on an explicit safe allowlist. A transient database
failure may occur after an unknown commit outcome, so an operation is not safe
to retry merely because the SQL text looks simple. The retry layer remains
nearest SQL; timing wraps the complete cache-miss path; and cache sits outside
both so a hit does not pretend database work happened.

## Maintain Store Conformance

Each Store contract’s reusable conformance suite is the executable statement
of its portable behavior. Add cases for:

- invalid input and exact absence semantics;
- successful and no-op outcomes;
- stale revisions and competing writers;
- idempotent replay, conflict, and in-progress states;
- required audit failure;
- database-time expiry or lease fences; and
- unknown-outcome or adapter error translation.

The PostgreSQL adapter must pass the same observable suite as any test or
memory adapter. Add a PostgreSQL-specific race test where the guarantee depends
on actual locking or constraints rather than a sequential fake.

## Current Pre-Release Migration Rule

The repository currently owns one rewritable schema baseline:

```text
server/migrations/postgres/000001_baseline.up.sql
server/migrations/postgres/000001_baseline.down.sql
```

Before the first stable release, incompatible development schemas are not
upgraded. Schema work updates that baseline and its round-trip tests; affected
developer databases must be discarded and recreated. Do not add a chain of
historical development migrations that pretends to be a supported upgrade
contract.

Reset the repository-owned local environment with:

```sh
make dev-reset
```

The command deletes local developer containers and volumes. Preserve anything
you need before running it.

{/* TODO(migration-policy): Before the first stable release, define and
publish the append-only migration, compatibility window, rollback, backup, and
mixed-version deployment policy. Replace the rewritable-baseline instructions
only after that production decision is accepted. */}

## Run Focused PostgreSQL Tests

Start with the affected package or conformance test, then run the repository
target that provisions and removes its PostgreSQL dependency:

```sh
make -C server integration-postgres
```

The target covers Store conformance, migrations, application transactions,
identity and authorization races, Jobs, files, mail, examinations, and other
PostgreSQL-backed behavior. An invoked integration suite fails when PostgreSQL
is unavailable rather than skipping.

The hermetic server gate remains required:

```sh
make -C server check
```

## Treat Caches as Adapters

The Store read-through cache is a bounded local optimization. The application
cache may use memory or Redis. Neither changes PostgreSQL authority.

Commit durable state first, then invalidate or publish. A missed invalidation
must recover through bounded TTL, current reads, revalidation, or client
resynchronization. Do not use cache presence as proof of authorization or
cluster delivery as proof that another node observed a commit.

## Review Persistence Safety

- Are lists, scans, retries, and retained fields bounded?
- Does a named operation expose the complete atomic guarantee?
- Are current authority and database-time conditions checked inside the
  transaction where races matter?
- Do constraints protect invariants even when two nodes act concurrently?
- Are retries safe against unknown commit outcomes?
- Do logs, metrics, errors, and audit data omit SQL text, secrets, answers,
  content, and uncontrolled values?
- Does the change pass migration round-trip and the affected conformance suite?

## Next

- Coordinate [application behavior](./server-workflow.mdx).
- Add or update the [HTTP contract](./http-and-openapi.mdx).
- Choose the [integration and CI gates](./testing.mdx).
